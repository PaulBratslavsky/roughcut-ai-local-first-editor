// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { Aggressiveness } from "./Aggressiveness";
import type { Clip } from "./Clip";
import type { Padding } from "./Padding";

/**
 * An edit, as data. Every mutation of the cut — from the UI, the local
 * agent, or an MCP client — is one of these values applied through a single
 * Editor entry point. Ops are serializable, so the undo journal and the
 * audit trail are replayable.
 */
export type EditOp = { "type": "cut_range", start: number, end: number, } | { "type": "restore_range", start: number, end: number, } | { "type": "cut_segments", segment_ids: Array<string>, } | { "type": "restore_segments", segment_ids: Array<string>, } | { "type": "trim_clip", clip_id: string, new_source_in: number, new_source_out: number, } | { "type": "split_clip", clip_id: string, at_time: number, } | { "type": "reorder_clip", clip_id: string, new_order: number, } | { "type": "set_global_padding", start_s: number, end_s: number, linked: boolean, } | { "type": "rough_cut", aggressiveness: Aggressiveness, } | { "type": "set_clips", clips: Array<Clip>, global_padding: Padding, };
