{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities: core IPC + events + asset protocol for video preview.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
